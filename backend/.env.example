# =================================================================
# 백엔드 환경 변수 설정 (Development)
# =================================================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요.
# =================================================================

# ===== 데이터베이스 설정 (MySQL) =====
DB_USER=root
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=3306
DB_NAME=capstone

# ===== Redis 설정 =====
# Redis URL (Upstash 등 클라우드 Redis 사용 시)
# REDIS_URL=redis://default:password@host:port

# 로컬 Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ===== 이메일 설정 (SMTP) =====
# Gmail 사용 시: smtp.gmail.com
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Gmail 앱 비밀번호 생성 방법:
# 1. Google 계정 관리 > 보안
# 2. 2단계 인증 활성화
# 3. 앱 비밀번호 생성
# 4. 생성된 16자리 비밀번호를 EMAIL_PASSWORD에 입력

# ===== 카카오 OAuth 설정 =====
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_REDIRECT_URI=http://localhost:3000/auth/kakao/callback

# 카카오 개발자 콘솔: https://developers.kakao.com
# 1. 내 애플리케이션 > 앱 키 > REST API 키
# 2. 플랫폼 설정 > Web > 사이트 도메인 등록
# 3. 카카오 로그인 > Redirect URI 등록

# 세션 만료 시간 (초 단위)
SESSION_EXPIRE_SECONDS=86400

# ===== 애플리케이션 설정 =====
DEV_MODE=false

# 앱 설정
BASE_URL=http://localhost:8000

# ===== 세션 및 보안 설정 =====
SECRET_KEY=your-secret-key-change-this-in-production
SESSION_SECRET_KEY=your-session-secret-key